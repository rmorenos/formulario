let extructuta = [];
function addOjecto(nombre, apellido, hm, edad, hobbit) {
    let newObject = {
        nombre: nombre,
        apellido: apellido,
        hm: hm,
        edad: edad,
        hobbit: hobbit
    };
    console.log(newObject);
    extructuta.push(newObject);
}

